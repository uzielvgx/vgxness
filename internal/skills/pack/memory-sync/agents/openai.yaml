interface:
  display_name: "Memory Sync"
  short_description: "Safely synchronize VGXNESS client memory"
  default_prompt: "Use $memory-sync to synchronize VGXNESS memory safely."
policy:
  allow_implicit_invocation: true
