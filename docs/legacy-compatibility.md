# Legacy Compatibility Matrix

This matrix records evidence-bound compatibility surfaces. Lifecycle labels distinguish **Current runtime** behavior, **Upgrade compatibility** for forward migration, **Historical recognition/repair** for exact known historical state, **Historical retirement** for exact removal-only handling, and **Current validation/recovery** for present manifest checks. No release status or retirement date is implied.

| Surface | Lifecycle state and purpose | Source of truth | Evidence/tests | Removal prerequisite | Owner/review trigger |
| --- | --- | --- | --- | --- | --- |
| OpenCode model-plan v1 | Historical recognition/repair; strictly decodes and regenerates recognized homogeneous manifests, including known historical/predecessor bundles. | `internal/providers/opencode/model_plan.go`; `internal/sdd/model_config.go` | `internal/providers/opencode/integration_test.go`; `internal/providers/opencode/manager_test.go`. | Explicit compatibility decision with accepted-manifest migration or rejection evidence. | OpenCode integration owner; review when manifest decoding or bundle rendering changes. |
| OpenCode model-plan v2 | Historical recognition/repair; strictly decodes and regenerates recognized mixed-provider manifests; unchanged installed v2 plans are retained. | `internal/providers/opencode/model_plan.go`; `internal/sdd/model_config.go` | `internal/providers/opencode/integration_test.go`; `internal/providers/opencode/manager_test.go`. | Explicit compatibility decision with accepted-manifest migration or rejection evidence. | OpenCode integration owner; review when manifest decoding or bundle rendering changes. |
| OpenCode model-plan v3 | Current runtime; current per-agent assignment manifest. | `internal/providers/opencode/model_plan.go` | OpenCode model-plan and setup tests. | Replacement schema plus evidence for all installed v3 plans. | OpenCode integration owner; review when assignment schema changes. |
| Historical OpenCode bundles, templates, and agents | Historical recognition/repair; exact v54 and v53 predecessor packages plus older managed artifact bytes prevent unsafe overwrite or loss of installed configuration. | Embedded managed artifacts and OpenCode integration render/validation code. | OpenCode integration, manager, and validation tests. | Exact inventory, migration or safe rejection evidence, and explicit compatibility decision. | OpenCode integration owner; review when managed artifacts change. |
| Historical OpenCode plugin (`vgxness.ts` v1-v10) | Historical retirement; the observed contract retires exact v1-v10 plugin bytes only; modified, foreign, unknown, or newer bytes block removal. | `internal/providers/opencode`; `internal/setup`. | `internal/providers/opencode/integration_test.go`; `internal/setup/setup_test.go`. | Explicit compatibility decision with exact-byte retirement or safe-blocking evidence. | OpenCode and setup owners; review when plugin retirement handling changes. |
| Codex legacy packages | Historical recognition/repair; exact v14 and v13 static package recognition supports status, inspection, reinstall, and recovery, not general import. | `internal/providers/codex/integration.go`; `internal/providers/codex/render.go`. | `internal/providers/codex/integration_test.go`; `internal/providers/codex/render_test.go`. | Explicit compatibility decision with recognized-package migration or safe rejection evidence. | Codex integration owner; review when package recognition or recovery changes. |
| Codex current package | Current runtime; renders the current standalone Codex projection and validates its artifact digest. | `internal/providers/codex` package renderer and validator. | Codex render and integration tests. | Replacement package contract and validation evidence. | Codex integration owner; review when rendered profile format changes. |
| SQLite schema v1-v11 | Upgrade compatibility; forward-only local storage migration history, including the frozen V10 cumulative physical schema identity and v11 ultra model-plan migration. | `internal/memory/migrations.go` and embedded `internal/memory/migrations/001_*.sql` through `011_*.sql`. | Memory migration, health, and upgrade/restart tests. | Explicit compatibility decision, migration path or rejection evidence, and replacement identity test. | Memory storage owner; review for any historical migration-byte edit. |
| SQLite schema v13 | Predecessor portable project identity provenance: portable UUID to unchanged local project ID, workspace hash, source, and timestamp. UUID-shaped legacy project IDs are never classified by syntax. | `013_project_identities.sql` | Migration and explicit-init tests. | Forward migration to v14. | Memory storage owner. |
| SQLite schema v14 | Predecessor outbound portable session/observation wire identities with retained first-origin device provenance. | `014_sync_portable_identities.sql` | Migration, Health, translation, and race tests. | Forward-only migration to v15. | Memory storage owner. |
| SQLite schema v15 | Current inbound portable-identity adoption foundation: an adopted session or observation stores its wire UUID, adopting device, and time beside the mapping. It does not materialize pulled records. | `015_sync_portable_identity_adoptions.sql` | Migration, Health, adoption, tamper, and race tests. | Forward-only migration evidence. | Memory storage owner. |
| SQLite schema v12 | Predecessor and upgrade compatibility; sync-enrollment recovery schema with a bounded previous credential-reference marker, never a bearer. | `internal/memory/migrations.go` and `012_sync_enrollment_recovery.sql`. | Memory migration, health, and sync-enrollment recovery tests. | Replacement migration and upgrade/restart evidence. | Memory storage owner; review when SQLite schema advances. |
| PostgreSQL schema v1-v3 | Upgrade compatibility; forward-only sync-service schema ledger and fingerprint history. | `internal/syncpg/migrate.go` and embedded `internal/syncpg/migrations/*.sql`. | Sync PostgreSQL migration validation and repository tests. | Explicit compatibility decision with ledger, checksum, and fingerprint migration or rejection evidence. | Sync service owner; review when PostgreSQL migrations change. |
| Historical portable skills and digests | Historical recognition/repair; exact predecessor and legacy digests recognize managed content and classify unknown bytes as drift. `vgxness skills status --compatibility` is an independent, read-only inventory of declared legacy paths: absent, recognized for any lifecycle-accepted exact content, modified-or-unknown, or conflict. | `internal/skills/catalog.go`; `internal/skills/skills.go`. | Skills catalog, lifecycle, drift, recovery, and compatibility inventory tests. | Exact digest inventory plus compatibility decision and focused recognition/drift evidence. | Skills owner; review when catalog or predecessor digests change. |
| Launcher and self-install manifests | Current validation/recovery; current manifests validate launcher identity, active digests, and recovery data. Historical manifest-version compatibility is not evidenced. | `internal/launcher/launcher.go`; `internal/selfinstall/selfinstall.go`. | Launcher, self-install, recovery, and manifest validation tests. | Explicit manifest migration/rejection evidence and recovery-path verification. | Installer owner; review when manifest fields or atomic activation change. |

## Review rule

Historical bytes, digests, and schema migrations are compatibility evidence. Changing or removing one requires an explicit compatibility decision and focused evidence before it can be considered removable.

## Portable sync compatibility

Legacy local identifiers are not rewritten for sync. Outbound project-scoped transport uses durable deterministic wire-only identities instead; `resolve`, pull, and bootstrap translation remain unsupported.

## CARE compatibility boundary

CARE has no current fixed-lens aliases. Exact predecessors are recognized only for lifecycle and upgrade compatibility, never as current markers. [CARE architecture](care.md) defines those markers; [CARE evaluation](care-evaluation.md) distinguishes static repository checks from external adjudication.
